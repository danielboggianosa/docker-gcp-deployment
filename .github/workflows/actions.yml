name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.5.0
      with:
        key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
        username: dev.danielboggiano
        host: 34.27.119.80
        port: 22
        script: |
        sudo sh /home/dev.danielboggiano/docker-gcp-deployment/deploy.sh
